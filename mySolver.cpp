#include <vector>
#include <math.h>
#include "myMatrix.h"
#include "mySolver.h"

bool
jacobi(const dnMatrix& a, const Vector& b)
{
   /* need to be worked on */
   size_t n = b.size();
   Vector init(n);
   //while(norm(a*x - b) > pow(10, -5)) {
   // 
   //} 
   return true;
}
